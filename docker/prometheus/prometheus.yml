global:
  scrape_interval: 15s

scrape_configs:
  - job_name: exchange-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - exchange-api:8080

  - job_name: jenkins
    metrics_path: /prometheus/
    basic_auth:
      username: soumitra
      password: deshpande
    static_configs:
      - targets:
          - jenkins:8080
